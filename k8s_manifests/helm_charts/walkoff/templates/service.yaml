apiVersion: v1
kind: Service
metadata:
  name: {{ template "walkoff.fullname" . }}
  labels:
    app: {{ template "walkoff.name" . }}
    chart: {{ template "walkoff.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "walkoff.name" . }}
    release: {{ .Release.Name }}
