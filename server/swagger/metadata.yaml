/availablesubscriptions:
  get:
    tags:
      - System
    summary: Gets available subscription options
    description: ''
    operationId: server.endpoints.metadata.get_possible_subscriptions
    produces:
      - application/json
    responses:
      200:
        description: Success

/filters:
  get:
    tags:
      - System
    summary: Displays all filters
    description: ''
    operationId: server.endpoints.metadata.get_filters
    produces:
      - application/json
    responses:
      200:
        description: Success

/flags:
  get:
    tags:
      - System
    summary: Displays all flags
    description: ''
    operationId: server.endpoints.metadata.get_flags
    produces:
      - application/json
    responses:
      200:
        description: Success

/interface/{interface_name}:
  post:
    tags:
      - System
    summary: Render interface
    description: ''
    operationId: server.endpoints.metadata.sys_pages
    produces:
      - application/json
    parameters:
      - name: interface_name
        in: path
        description: The name that needs to be fetched.
        required: true
        type: string
      - in: body
        name: widget_args
        description: Pass page and arguments to render widget.
        required: false
        schema:
          $ref: '#/definitions/RenderArgs'
    responses:
      200:
        description: Success

/key:
  get:
    tags:
      - System
    summary: Displays api key
    description: ''
    operationId: server.endpoints.metadata.login_info
    produces:
      - application/json
    responses:
      200:
        description: Success

/widgets:
  get:
    tags:
      - System
    summary: Displays all widgets
    description: ''
    operationId: server.endpoints.metadata.get_widgets
    produces:
      - application/json
    responses:
      200:
        description: Success