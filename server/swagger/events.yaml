'/events/{event_id}':
  post:
    tags:
      - Events
    summary: Edit an event note
    description: ''
    operationId: server.endpoints.events.edit_event_note
    produces:
      - application/json
    parameters:
      - name: event_id
        in: path
        description: The name that needs to be fetched.
        required: true
        type: string
      - in: body
        name: config_value
        description: Value for configuration key
        required: true
        schema:
          $ref: '#/definitions/event_note'
    responses:
      '200':
        description: Okay